import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['picsum.photos', 'www.w3schools.com'], // Add picsum.photos and www.w3schools.com for the video thumbnail
  },
  /* config options here */
};

export default nextConfig;